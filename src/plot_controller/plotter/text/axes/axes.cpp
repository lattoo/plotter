#include "axes.h"

AxesText::AxesText()
{

}

void AxesText::replot(QPainter* painter, const QRect& plot_area)
{
//    _label.axis_x().replot(painter, plot_area);
//    _label.axis_y().replot(painter, plot_area);

//    _title.title_x().replot(painter, plot_area);
//    _title.title_y().replot(painter, plot_area);
}
